The algorithm processes the incoming text stream and analyzes the word frequency. When the algorithm finds repeating patterns, the algorithm assigns a token to each repeating word. This process allows the algorithm to reduce the total size of the text stream. Using a good algorithm ensures that the compression ratio remains high while the overall algorithm execution speed stays fast.